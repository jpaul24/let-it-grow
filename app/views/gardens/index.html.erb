<div class="my-container">
  <div class="text-center index-header">
    <h1>Available Gardens</h1>
  </div>
  <div class="row">
  <% @gardens.each do |garden| %>
    <div class="col-3">
      <%= link_to garden_path(garden) do  %>
        <div class="index-card">
          <div class="garden-image" style="background-image: url('https://picsum.photos/200')">
          </div>
          <div class="card-info">
            <div>
              <h2><%= garden.name %></h2>
            </div>
            <div class="card-info-small">
              <p>£<%= garden.price_per_day %>/day</p>
              <p><%= garden.location %></p>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  </div>
</div>
